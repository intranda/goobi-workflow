(function () {
    'use strict';

    const constrainWidth = function constrainWidth(col, newWidth) {
        let constrainedWidth;
        const maxWidth = getComputedStyle(col).getPropertyValue('max-width');
        let minWidth = getComputedStyle(col).getPropertyValue('min-width');
        minWidth = minWidth === "0px" ? "60px" : minWidth;
        constrainedWidth = Math.max(parseInt(minWidth, 10), newWidth);
        if (maxWidth !== 'none') {
            constrainedWidth = Math.min(parseInt(maxWidth, 10), newWidth);
        }
        return constrainedWidth;
    };
    const resizeHandles = function resizeHandles(col) {
        const table = col.closest('.table-resizable');
        const tableHeight = table.offsetHeight;
        const handles = col.parentElement.querySelectorAll('.resize-handle');
        [...handles].forEach((handle) => {
            handle.style.setProperty('--handle-height', `${tableHeight}px`);
        });
    };
    const getRelWidth = function getRelativeWidth(col) {
        const table = col.closest('.table-resizable');
        const tableWidth = table.offsetWidth;
        const relWidth = (col.offsetWidth / tableWidth) * 100;
        return relWidth.toFixed(2);
    };
    const saveColWidths = function saveColWidths(col) {
        const table = col.closest('.table-resizable');
        if (!table.id) return;
        const firstRow = table.getElementsByTagName('tr')[0];
        const colWidths = [];
        const cols = firstRow ? firstRow.children : undefined;
        if (!cols) return;
        [...cols].forEach((c) => {
            if (c.id) {
                const colInfo = {
                    id: c.id,
                    width: `${getRelWidth(c)}%`,
                };
                colWidths.push(colInfo);
            }
        });
        localStorage.setItem(table.id, JSON.stringify(colWidths));
    };
    const loadColWidths = function loadColWidths(table) {
        const storage = JSON.parse(localStorage.getItem(table.id));
        if (storage) {
            storage.forEach((col) => {
                try {
                    document.getElementById(col.id).style.width = col.width;
                    return 0;
                } catch (error) {
                    return 1;
                }
            });
        }
    };
    const setRelWidth = function setRelativeWidth(col) {
        const column = col;
        column.style.width = `${getRelWidth(col)}%`;
    };
    const createHandle = function createHandle() {
        const div = document.createElement('div');
        div.classList.add('resize-handle');
        return div;
    };
    const setListeners = function setListeners(tableWidth, handle) {
        let pageX;
        let col;
        let colWidth;
        let newWidth;
        let nextCol;
        let nextColWidth;
        let nextColNewWidth;
        let diffX;
        let totalWidth;
        handle.addEventListener('click', (e) => {
            if (e.detail === 2) {
                col = e.target.parentElement;
                colWidth = col.offsetWidth;
                nextCol = col.nextElementSibling;
                nextColWidth = nextCol.offsetWidth;
                totalWidth = colWidth + nextColWidth;
                newWidth = constrainWidth(col, 60);
                nextColNewWidth = totalWidth - newWidth;
                if (totalWidth === (newWidth + nextColNewWidth)) {
                    newWidth = (newWidth / tableWidth) * 100;
                    nextColNewWidth = (nextColNewWidth / tableWidth) * 100;
                    col.style.width = `${newWidth.toFixed(2)}%`;
                    nextCol.style.width = `${nextColNewWidth.toFixed(2)}%`;
                }
                col = undefined;
                colWidth = undefined;
                newWidth = undefined;
                nextCol = undefined;
                nextColWidth = undefined;
                nextColNewWidth = undefined;
                totalWidth = undefined;
            }
        });
        handle.addEventListener('mousedown', (e) => {
            col = e.target.parentElement;
            pageX = e.pageX;
            colWidth = col.offsetWidth;
            nextCol = col.nextElementSibling;
            nextColWidth = nextCol.offsetWidth;
            totalWidth = colWidth + nextColWidth;
        });
        document.addEventListener('mousemove', (e) => {
            if (col) {
                diffX = e.pageX - pageX;
                if (diffX === 0) {
                    return;
                }
                handle.classList.add('active');
                resizeHandles(col);
                document.getElementsByTagName('body')[0].classList.add('table-resizing');
                nextColNewWidth = nextColWidth - diffX;
                nextColNewWidth = constrainWidth(nextCol, nextColNewWidth);
                newWidth = colWidth + diffX;
                newWidth = constrainWidth(col, newWidth);
                if (totalWidth === (newWidth + nextColNewWidth)) {
                    newWidth = (newWidth / tableWidth) * 100;
                    nextColNewWidth = (nextColNewWidth / tableWidth) * 100;
                    col.style.width = `${newWidth.toFixed(2)}%`;
                    nextCol.style.width = `${nextColNewWidth.toFixed(2)}%`;
                }
            }
        });
        document.addEventListener('mouseup', () => {
            if (col) {
                handle.classList.remove('active');
                saveColWidths(col);
            }
            document.getElementsByTagName('body')[0].classList.remove('table-resizing');
            pageX = undefined;
            col = undefined;
            colWidth = undefined;
            newWidth = undefined;
            nextCol = undefined;
            nextColWidth = undefined;
            nextColNewWidth = undefined;
            diffX = undefined;
            totalWidth = undefined;
        });
    };
    const initialize = function initializeResizeTable(table) {
        const firstRow = table.getElementsByTagName('tr')[0];
        const cols = firstRow ? firstRow.children : undefined;
        if (!cols) return;
        [...cols].forEach((col) => {
            const column = col;
            const tableWidth = table.offsetWidth;
            const handle = createHandle();
            resizeHandles(col);
            setRelWidth(col);
            column.appendChild(handle);
            column.classList.add('resize-col');
            setListeners(tableWidth, handle);
        });
    };
    const init$1 = function initializeResizeTables() {
        const tables = document.querySelectorAll('.table-resizable');
        if (tables) {
            [...tables].forEach((table) => {
                loadColWidths(table);
                initialize(table);
            });
        }
    };

    const findTooltips = function findTooltips (scope = document, selectors = '[data-bs-toggle="tooltip"], [data-toggle="tooltip"]') {
      return [...scope.querySelectorAll(selectors)]
    };
    const initTooltips = function initTooltips(scope = document) {
      const tooltipTriggers = findTooltips(scope);
      tooltipTriggers.map(function (tooltipTriggerEl) {
        return hoverableTooltip(tooltipTriggerEl)
      });
    };
    const initPopovers = function initPopovers() {
      const selector = '[data-bs-toggle="popover"]';
      const popoverTargets = findTooltips(document, selector);
      popoverTargets.map(function (popoverTriggerEl) {
        return hoverablePopover(popoverTriggerEl)
      });
    };
    const hideAllTooltips = (element) => {
      const tooltips = document.querySelectorAll('[aria-describedby*="tooltip"]');
      tooltips.forEach((tooltip) => {
        const tooltipId = tooltip.getAttribute('aria-describedby');
        const tooltipElement = document.getElementById(tooltipId);
        const tooltipInstance = bootstrap.Tooltip.getInstance(tooltipElement);
        if (tooltip != element && tooltipInstance) {
          tooltipInstance.hide();
        }
      });
    };
    const hideAllPopovers = () => {
      const popovers = document.querySelectorAll('[aria-describedby*="popover"]');
      popovers.forEach((popover) => {
        const popoverId = popover.getAttribute('aria-describedby');
        const popoverElement = document.getElementById(popoverId);
        const popoverInstance = bootstrap.Popover.getInstance(popoverElement);
        if (popoverInstance) {
          popoverInstance.hide();
        }
      });
    };
    const hoverableTooltip = function keepTooltipOpenOnHoverOverContent(element) {
      let tooltip = new bootstrap.Tooltip(element, {
        trigger: 'manual',
        offset: [4, 4],
      });
      let tooltipHideTimeout;
      let tooltipShowTimeout;
      element.addEventListener('click', (event) => {
        event.preventDefault;
        event.stopPropagation;
        hideAllTooltips();
      });
      ['mouseenter', 'focusin'].forEach((eventType) => {
        element.addEventListener(eventType, (event) => {
          event.preventDefault;
          event.stopPropagation;
          hideAllTooltips(element);
          clearTimeout(tooltipHideTimeout);
          tooltipShowTimeout = setTimeout(() => {
            tooltip?.show();
            tooltip?.tip?.addEventListener('mouseleave', () => {
              event.preventDefault;
              event.stopPropagation;
              tooltipHideTimeout = timeout(tooltip, element, 200);
            });
            window.addEventListener('keydown', (event) => {
              event.preventDefault;
              event.stopPropagation;
              if (event.key === 'Escape') {
                tooltip.hide();
              }
            });
          }, 500);
          element.addEventListener('mouseleave', () => {
            clearTimeout(tooltipShowTimeout);
          });
        });
      });
      ['mouseleave', 'focusout'].forEach((eventType) => {
        element.addEventListener(eventType, (event) => {
          event.preventDefault;
          event.stopPropagation;
          tooltipHideTimeout = timeout(tooltip, element, 200);
        });
      });
      return tooltip;
    };
    const hoverablePopover = function keepPopoverOpenOnHoverOverContent(element) {
      let popover = new bootstrap.Popover(element, {
        trigger: 'manual'
      });
      let popoverHideTimeout;
      let popoverShowTimeout;
      ['mouseenter', 'focusin'].forEach((eventType) => {
        element.addEventListener(eventType, (event) => {
          event.preventDefault;
          event.stopPropagation;
          hideAllPopovers();
          clearTimeout(popoverHideTimeout);
          popoverShowTimeout = setTimeout(() => {
            popover.show();
            popover?.tip?.addEventListener('mouseleave', () => {
              event.preventDefault;
              event.stopPropagation;
              popover.hide();
            });
            window.addEventListener('keydown', (event) => {
              event.preventDefault;
              event.stopPropagation;
              if (event.key === 'Escape') {
                popover.hide();
              }
            });
          }, 500);
          element.addEventListener('mouseleave', () => {
            clearTimeout(popoverShowTimeout);
          });
        });
      });
      ['mouseleave', 'focusout'].forEach((eventType) => {
        element.addEventListener(eventType, (event) => {
          event.preventDefault;
          event.stopPropagation;
          popoverHideTimeout = timeout(popover, element, 200);
        });
      });
      return popover;
    };
    const timeout = function setTimeoutForHiding(target, element, duration) {
      setTimeout(() => {
        if (!target.tip?.matches(':hover') && !element.matches(':hover')) {
          target.hide();
        }
      }, duration);
    };
    const init = function initTooltipsAndPopovers() {
      initTooltips();
      initPopovers();
    };

    const defaultSelector$1 = '[data-bs-toggle="tooltip"]';
    const config$1 = {
      attributes: true,
      childList: false,
      subtree: false,
    };
    const handleTooltip = function handleTooltip(element) {
      const tooltip = bootstrap.Tooltip.getInstance(element);
      const dropdownToggle = element.querySelector(".dropdown-toggle");
      let dropdownIsExpanded = dropdownToggle.getAttribute('aria-expanded');
      const observer = new MutationObserver((mutationsList, observerInstance) => {
        mutationsList.forEach((mutation) => {
          if (mutation.attributeName === 'aria-expanded') {
            dropdownIsExpanded = dropdownToggle.getAttribute('aria-expanded');
            if (dropdownIsExpanded === 'false') {
              tooltip.disable();
            }
            if (dropdownIsExpanded === 'true') {
              tooltip.enable();
              observerInstance.disconnect();
            }
          }
        });
      });
      observer.observe(dropdownToggle, config$1);
      element.addEventListener('click', (event) => {
        dropdownIsExpanded = dropdownToggle.getAttribute('aria-expanded');
        if (dropdownIsExpanded === 'true') {
          tooltip.disable();
          observer.observe(dropdownToggle, config$1);
        } else {
          tooltip.enable();
        }
      });
    };
    const dropdownTooltips = function toggleDropdownTooltips(selector = defaultSelector$1) {
      const elements = document.querySelectorAll(selector);
      elements.forEach((element) => {
        if (element.querySelector('.dropdown-toggle')) {
          handleTooltip(element);
        }
      });
    };

    const defaultSelector = '[data-gw-focus-on-load="true"]';
    const focusOnLoad = function focusOnLoad(selector = defaultSelector) {
      window.addEventListener("DOMContentLoaded", function() {
        try {
          const element = document.querySelector(selector);
          element?.focus();
        }
        catch {}
      });
    };

    const syncHelpState = function syncHelpState() {
        const helpButton = document.querySelector('[id$="showHelp"]');
        if (!helpButton) return;
        const shouldShow = helpButton.dataset.showHelp === 'true';
        const elements = document.querySelectorAll(".help-block");
        elements.forEach((el) => {
            el.style.display = shouldShow ? 'block' : 'none';
        });
    };
    const toggleHelp = function toggleHelp() {
        const helpButton = document.querySelector('[id$="showHelp"]');
        if (!helpButton) return;
        helpButton.addEventListener('click', function() {
            setTimeout(() => {
                syncHelpState();
            }, 100);
        });
        syncHelpState();
    };

    const initScrollTop = () => {
        let offset = 50;
        let scrollTop = document.querySelector('.js-scroll-top');
        let progressPath = document.querySelector('.js-scroll-top path');
        if (progressPath === null) {
            return;
        }
        let pathLength = progressPath.getTotalLength();
        let dashArray = pathLength + ' ' + pathLength;
        let dashOffset = pathLength;
        window.addEventListener("scroll", () => {
            let scroll = document.body.scrollTop || document.documentElement.scrollTop;
            let height = document.documentElement.scrollHeight - document.documentElement.clientHeight;
            dashOffset = pathLength - (scroll * pathLength / height);
            updateProgressPath(progressPath, dashOffset, dashArray);
            if (scroll > offset) {
                scrollTop.classList.add('active-progress');
            } else {
                scrollTop.classList.remove('active-progress');
            }
        });
    };
    const updateProgressPath = (path, offset, array) => {
        path.style.setProperty('--scrollProgess', offset);
        path.style.setProperty('--dashArray', array);
    };

    const initFocusOnClick = () => {
        const elements = document.querySelectorAll('[data-focus-on-click]');
        if (elements.length === 0) {
            return;
        }
        elements.forEach(element => {
            element.addEventListener('click', () => {
                const targetSelector = element.getAttribute('data-focus-on-click');
                if (!targetSelector) {
                    return;
                }
                if (targetSelector.startsWith('#')) {
                    const targetElement = document.querySelector('[id$="' + targetSelector.substring(1) + '"]');
                    targetElement?.focus();
                    return;
                }
                const targetElement = document.querySelector(targetSelector);
                if (targetElement) {
                    targetElement.focus();
                }
            });
        });
    };

    const findTargetElement = (target) => {
        const selectorType = target.substring(0, 1);
        const targetId = target.substring(1);
        if (selectorType === '.') {
            return document.querySelector('.' + targetId);
        }
        return document.querySelector('[id$="' + targetId + '"]');
    };
    const preventDefaultEnter = (event) => {
        if (event.key === 'Enter') {
            event.preventDefault();
            event.stopPropagation();
        }
    };
    const handleSimpleKeyDown = (event) => {
        preventDefaultEnter(event);
    };
    const handleSimpleKeyUp = (event, targetSelector) => {
        if (event.key === 'Enter') {
            event.preventDefault();
            event.stopPropagation();
            const targetElement = findTargetElement(targetSelector);
            if (targetElement) {
                targetElement.click();
            }
        }
    };
    const parseTargetsObject = (targetSelector) => {
        try {
            const parsedTargets = JSON.parse(targetSelector);
            if (Object.keys(parsedTargets).length === 0) {
                return null;
            }
            return parsedTargets;
        } catch (e) {
            console.error('Error parsing data-submit-on-enter JSON:', e);
            return null;
        }
    };
    const handleComplexKeyDown = (event, targetSelector, state) => {
        event.preventDefault();
        event.stopPropagation();
        const targetsObject = parseTargetsObject(targetSelector);
        if (!targetsObject) {
            return;
        }
        if (event.ctrlKey && targetsObject.ctrl) {
            state.combinatoryKeyPressed = true;
            state.combinatoryTargetEl = findTargetElement(targetsObject.ctrl);
            state.combinatoryTargetEl?.classList.add('btn-highlight');
        }
    };
    const handleCombinatoryRelease = (state) => {
        state.combinatoryKeyPressed = false;
        state.combinatoryTargetEl?.classList.remove('btn-highlight');
        state.combinatoryTargetEl = null;
    };
    const handleCombinatoryEnter = (event, targetsObject, state) => {
        event.preventDefault();
        event.stopPropagation();
        if (state.combinatoryTargetEl) {
            state.combinatoryTargetEl.click();
            state.combinatoryTargetEl.classList.remove('btn-highlight');
        }
        handleCombinatoryRelease(state);
    };
    const handleDefaultEnterSubmit = (event, targetsObject) => {
        event.preventDefault();
        event.stopPropagation();
        const targetElement = findTargetElement(targetsObject.default);
        if (targetElement) {
            targetElement.click();
        }
    };
    const handleComplexKeyUp = (event, targetSelector, state) => {
        const targetsObject = parseTargetsObject(targetSelector);
        if (!targetsObject) {
            return;
        }
        if (event.key === 'Control' || event.key === 'Meta') {
            handleCombinatoryRelease(state);
        } else if (event.key === 'Enter' && state.combinatoryKeyPressed && targetsObject.ctrl) {
            handleCombinatoryEnter(event, targetsObject, state);
        } else if (event.key === 'Enter' && !state.combinatoryKeyPressed && targetsObject.default) {
            handleDefaultEnterSubmit(event, targetsObject);
        }
    };
    const attachEventListeners = (element, targetSelector) => {
        const state = {
            combinatoryKeyPressed: false,
            combinatoryTargetEl: null
        };
        const idSelector = targetSelector.startsWith('#');
        element.addEventListener('keydown', (event) => {
            if (idSelector) {
                handleSimpleKeyDown(event);
            } else {
                handleComplexKeyDown(event, targetSelector, state);
            }
        });
        element.addEventListener('keyup', (event) => {
            if (idSelector) {
                handleSimpleKeyUp(event, targetSelector);
            } else {
                handleComplexKeyUp(event, targetSelector, state);
            }
        });
    };
    const initSubmitOnEnter = () => {
        const elements = document.querySelectorAll('[data-submit-on-enter]');
        if (elements.length === 0) {
            return;
        }
        elements.forEach(element => {
            const targetSelector = element.getAttribute('data-submit-on-enter');
            attachEventListeners(element, targetSelector);
        });
    };

    const initMirrorValueTo = () => {
        const elements = document.querySelectorAll('[data-mirror-value-to]');
        if (elements.length === 0) {
            return;
        }
        elements.forEach(element => {
            element.addEventListener('input', () => {
                const targetSelectors = JSON.parse(element.getAttribute('data-mirror-value-to'));
                if (!targetSelectors || targetSelectors.length === 0) {
                    return;
                }
                targetSelectors.forEach(targetSelector => {
                    if (!targetSelector) {
                        return;
                    }
                    if (targetSelector.startsWith('#')) {
                        const targetElement = document.querySelector('[id$="' + targetSelector.substring(1) + '"]');
                        if (targetElement) {
                            targetElement.value = element.value;
                        }
                        return;
                    }
                    const targetElement = document.querySelector(targetSelector);
                    if (targetElement) {
                        targetElement.value = element.value;
                    }
                });
            });
        });
    };

    const initAccessibilityModeButton = () => {
        const accessibilityButton = document.querySelector('[data-toggle-accessibility-mode]');
        if (!accessibilityButton) {
            return;
        }
        accessibilityButton.addEventListener('click', (event) => {
            document.body.classList.toggle('accessibility-mode');
        });
    };

    const settings$3 = {
        tileSources: "#tileSource",
        persistence: "#persistenceId",
        controls: {
            zoomInput: "#zoomSliderLabel input",
            rotateRight: "#rotate-right-x",
            rotateLeft: "#rotate-left-x",
            reset: "#reset-position"
        },
        imageView: {
            element: "#pageContentRight #mainimagearea #mainImage",
            fittingMode: "fixed",
            margins: {bottom:0},
            sequence: {
                columns: 1,
                columnOffset: 0,
                rowMargin: 0
            }
        }
    };
    var loadedImage;
    var zoomControl;
    var rotationControl;
    var eventListeners = new Map();
    const loadImage = function() {
        if(typeof ImageView === "undefined" || !document.querySelector(settings$3.imageView.element)) {
            return;
        }
        if(!document.querySelector(settings$3.imageView.element)) {
            return;
        }
        if(loadedImage?.element?.isConnected) {
            return loadedImage;
        }
        if(!loadedImage) {
            loadedImage = new ImageView.Image(settings$3.imageView);
        } else if(!loadedImage.element.isConnected) {
            loadedImage.close();
            loadedImage = new ImageView.Image(settings$3.imageView);
        }
        if(zoomControl) {
            zoomControl.close();
        }
        if(rotationControl) {
            rotationControl.close();
        }
        rotationControl = new ImageView.Controls.Rotation(loadedImage);
        zoomControl = new ImageView.Controls.Zoom(loadedImage);
        zoomControl.setInput(settings$3.controls.zoomInput);
        new ImageView.ViewPersistence(zoomControl, "metadata_image_" + getValue(settings$3.persistence));
        eventListeners.forEach((listener, element) => {
            element?.removeEventListener("click", listener);
        });
        const turnLeftEvent = () => rotationControl.rotateLeft();
        const turnRightEvent = () => rotationControl.rotateRight();
        const resetEvent = () => {
            rotationControl.rotateTo(0);
            zoomControl.goHome();
        };
        eventListeners = new Map();
        eventListeners.set(getElement(settings$3.controls.rotateLeft), turnLeftEvent);
        eventListeners.set(getElement(settings$3.controls.rotateRight), turnRightEvent);
        eventListeners.set(getElement(settings$3.controls.reset), resetEvent);
        eventListeners.forEach((listener, element) => {
            element?.addEventListener("click", listener);
        });
        getElement(settings$3.controls.rotateLeft)?.addEventListener("click", turnLeftEvent);
        getElement(settings$3.controls.rotateRight)?.addEventListener("click", turnRightEvent);
        getElement(settings$3.controls.reset)?.addEventListener("click", resetEvent);
        const tileSource = getValue(settings$3.tileSources);
        loadedImage.load(tileSource).then(() => {
        });
        return loadedImage;
    };
    const getElement = function(obj) {
        if(typeof obj === "string") {
            return document.querySelector(obj);
        } else {
            return obj;
        }
    };
     const getValue = function(input) {
        const element = getElement(input);
        return element?.value || element?.innerHTML;
    };

    const settings$2 = {
        startSelectionButtons: ".start-area-edition",
        cancelSelectionButtons: ".cancel-area-edition",
        disableInteractionOverlay: "#disable-interaction-overlay",
        style: {
            borderWidth: 2,
            borderColor: "#ff4433",
            className: "areaselect-overlay"
        },
    };
    let areaSelect = undefined;
    let drawActive = false;
    let target = undefined;
    let subscriptions$2 = [];
    let oldImageElement = undefined;
    const initImageAreaCreation = function(image) {
        if(!image) {
            return;
        }
        subscriptions$2.forEach(s => s.unsubscribe());
        subscriptions$2 = [];
        const startSelectionButtons = document.querySelectorAll(settings$2.startSelectionButtons);
        const cancelSelectionButtons = document.querySelectorAll(settings$2.cancelSelectionButtons);
        if(startSelectionButtons.length) {
            subscriptions$2.push( rxjs.fromEvent( document.querySelectorAll(settings$2.startSelectionButtons), "click").subscribe(e => handleClickStart(e)));
        }
        if(cancelSelectionButtons.length) {
            subscriptions$2.push( rxjs.fromEvent( document.querySelectorAll(settings$2.cancelSelectionButtons), "click").subscribe(e => handleClickCancel()));
        }
        if(oldImageElement?.isConnected) {
            return;
        } else {
            oldImageElement = image.element;
        }
        if(areaSelect) {
            areaSelect.close();
        }
        areaSelect = new ImageView.AreaSelect(image, {
            removeOldOverlays : true,
            drawCondition: event => drawActive === true,
            transformCondition: event => true,
            drawStyle: settings$2.style,
        });
        areaSelect.finishedHook.subscribe(rect => {
            drawActive = false;
            rect = areaSelect.currentOverlay.getBoundsInImage();
            const area = createImageArea$1(rect);
            area.addTo = target;
            addPageArea(area);
            enableInteractions$1();
        });
    };
    function handleClickStart(event) {
        target = event.target.dataset.imageAreaTarget;
        console.log("init target ", event.target, target );
        drawActive = true;
        disableInteractions$1();
    }
    function handleClickCancel(event) {
        drawActive = false;
        cancelPageAreaEdition();
        enableInteractions$1();
    }
    function createImageArea$1(rect, id) {
        var area = {};
        if(rect) {
            area.areaId = id;
            area.x = Math.round(rect.x);
            area.y = Math.round(rect.y);
            area.w = Math.round(rect.width);
            area.h = Math.round(rect.height);
        }
        return area;
    }
    function disableInteractions$1() {
        document.querySelectorAll(".cancel-area-edition").forEach(btn => btn.style.display = "block");
        document.querySelectorAll(".start-area-edition").forEach(btn => btn.style.display = "none");
        const overlay = document.querySelector(settings$2.disableInteractionOverlay);
        if(overlay) {
            overlay.style.display = "block";
            document.querySelectorAll(".above-overlay, .above-overlay canvas").forEach(e => setZIndex$1(e, 1000));
        }
    }
    function enableInteractions$1() {
        console.log("disable overlay");
        document.querySelectorAll(".cancel-area-edition").forEach(btn => btn.style.display = "none");
        document.querySelectorAll(".start-area-edition").forEach(btn => btn.style.display = "block");
        const overlay = document.querySelector(settings$2.disableInteractionOverlay);
        if(overlay) {
            overlay.style.display = "none";
            document.querySelectorAll(".above-overlay, .above-overlay canvas").forEach(e => resetZIndex$1(e));
        }
    }
    function setZIndex$1(element, zIndex) {
        if(Number.isInteger(zIndex)) {
            const z = element.style.zIndex;
            if(z) {
                element.dataset.zIndex = z;
            }
            element.style.zIndex = zIndex;
        }
    }
    function resetZIndex$1(element) {
        const z = element.dataset.zIndex;
        if(z != undefined) {
            element.dataset.zIndex = "";
            element.style.zIndex = z;
        } else {
            element.style.zIndex = "auto";
        }
    }

    const settings$1 = {
        inputPageAreas: '[data-content="pageareas"]',
        buttonStartDeletion: "[data-pagearea-delete='start']",
        buttonCancelDeletion: "[data-pagearea-delete='cancel']",
        disableInteractionOverlay: "#disable-interaction-overlay",
        style: {
            borderWidth: 2,
            borderColor: "#ff4433",
            className: "areaselect-overlay",
            highlightClassName : "highlight"
        },
    };
    let overlays  = new Map();
    let subscriptions$1 = [];
    let deletionSubscriptions = [];
    let deleting = false;
    const initEditImageAreas = function(image) {
        if(!image) {
            return;
        }
        closeSubscriptions();
        const areaString = getValue(settings$1.inputPageAreas);
        const areas = areaString ? JSON.parse(areaString) : [];
        if(isViewerOpened(image)) {
            cleanupOverlays();
            drawAreas(areas, image);
            initAreaDeletion(image);
        } else {
            image.onOpened.subscribe( e => drawAreas(areas, image));
            image.onOpened.subscribe( e => initAreaDeletion(image));
        }
    };
    function initAreaDeletion(image) {
        const startButton = document.querySelector(settings$1.buttonStartDeletion);
        const cancelButton = document.querySelector(settings$1.buttonCancelDeletion);
        if(startButton && cancelButton) {
            subscriptions$1 = [
                    rxjs.fromEvent( startButton, "click" ).subscribe(e => startDeletionMode(image, startButton, cancelButton)),
                    rxjs.fromEvent( cancelButton, "click" ).subscribe(e => endDeletionMode(image, startButton, cancelButton)),
            ];
        }
    }
    function startDeletionMode(image, startButton, cancelButton) {
       hide(startButton);
       show(cancelButton);
       disableInteractions();
       deleting = true;
        overlays.values().forEach(overlay => {
            if(overlay.element?.classList.contains(settings$1.style.highlightClassName)) {
                overlay.element.style.cursor = "not-allowed";
                deletionSubscriptions.push(overlay.onClick().subscribe(e => {
                    overlay.remove();
                    deletePageArea(createImageArea(overlay.bounds, overlay.id));
                    endDeletionMode(image, startButton, cancelButton);
                }));
            }
        });
    }
    function endDeletionMode(image, startButton, cancelButton) {
        hide(cancelButton);
        show(startButton);
        enableInteractions();
        deleting = false;
        overlays.values().forEach(overlay => {
            if(overlay.element) {
                overlay.element.style.cursor = "auto";
            }
        });
        deletionSubscriptions.forEach(subscr => subscr.unsubscribe());
        deletionSubscriptions = [];
    }
    function closeSubscriptions() {
        subscriptions$1.forEach(subscr => subscr.unsubscribe());
        subscriptions$1 = [];
        deletionSubscriptions.forEach(subscr => subscr.unsubscribe());
        deletionSubscriptions = [];
    }
    function drawAreas(areas, image) {
        for(let area of areas) {
            const rect = new OpenSeadragon.Rect(parseInt(area.x), parseInt(area.y), parseInt(area.w), parseInt(area.h));
    	    const displayRect = ImageView.CoordinateConversion.scaleToOpenSeadragonCoordinates(rect, image.openseadragon, image.getOriginalImageSize());
            const overlay = new ImageView.Overlay(displayRect, {style: settings$1.style}, area.areaId);
            overlay.draw(image);
            overlay.transform = new ImageView.Transform(image, overlay, {
            	startCondition: () => !deleting && isHighlighted(overlay)
            });
            overlay.transform.finishedTransforming().subscribe(o => {
                const viewportBounds = o.getBounds();
                const imageBounds = o.convertFromViewportToImage(viewportBounds);
                const updatedArea = createImageArea(imageBounds, o.id);
                setPageArea(updatedArea);
            });
            if(area.highlight) {
                highlight(overlay);
            }
            overlays.set(area.areaId, overlay);
        }
    }
    function isHighlighted(overlay) {
        return overlay?.element?.classList.contains(settings$1.style.highlightClassName);
    }
    function cleanupOverlays() {
        overlays.values().forEach(o => {
            o.transform?.close();
            o.remove();
        });
        overlays = new Map();
    }
    function highlight(area) {
        if(typeof area === "string") {
            area = overlays.get(area);
        }
        overlays.values().forEach(o => o.element.classList.remove(settings$1.stylehighlightClassName));
        if(area) {
            area.element.classList.add(settings$1.style.highlightClassName);
        }
    }
    function isViewerOpened(image) {
        return image?.extent?.canvasSize?.x > 0 && image?.tileSources;
    }
    function createImageArea(rect, id) {
        var area = {};
        if(rect) {
            area.areaId = id;
            area.x = Math.round(rect.x);
            area.y = Math.round(rect.y);
            area.w = Math.round(rect.width);
            area.h = Math.round(rect.height);
        }
        return area;
    }
    function hide(element) {
        const currentDisplay = element.style.display;
        element.dataset.defaultDisplay = currentDisplay;
        element.style.display = "none";
    }
    function show(element) {
        element.style.display = element.dataset.defaultDisplay ?? "block";
        element.dataset.defaultDisplay = undefined;
    }
    function disableInteractions() {
        const overlay = document.querySelector(settings$1.disableInteractionOverlay);
        if(overlay) {
            overlay.style.display = "block";
            document.querySelectorAll(".above-overlay, .above-overlay canvas").forEach(e => setZIndex(e, 1000));
        }
    }
    function enableInteractions() {
        const overlay = document.querySelector(settings$1.disableInteractionOverlay);
        if(overlay) {
            overlay.style.display = "none";
            document.querySelectorAll(".above-overlay, .above-overlay canvas").forEach(e => resetZIndex(e));
        }
    }
    function setZIndex(element, zIndex) {
        if(Number.isInteger(zIndex)) {
            const z = element.style.zIndex;
            if(z) {
                element.dataset.zIndex = z;
            }
            element.style.zIndex = zIndex;
        }
    }
    function resetZIndex(element) {
        const z = element.dataset.zIndex;
        if(z != undefined) {
            element.dataset.zIndex = "";
            element.style.zIndex = z;
        }
    }

    const isff = typeof navigator !== 'undefined' ? navigator.userAgent.toLowerCase().indexOf('firefox') > 0 : false;
    function addEvent(object, event, method, useCapture) {
      if (object.addEventListener) {
        object.addEventListener(event, method, useCapture);
      } else if (object.attachEvent) {
        object.attachEvent("on".concat(event), method);
      }
    }
    function removeEvent(object, event, method, useCapture) {
      if (object.removeEventListener) {
        object.removeEventListener(event, method, useCapture);
      } else if (object.detachEvent) {
        object.detachEvent("on".concat(event), method);
      }
    }
    function getMods(modifier, key) {
      const mods = key.slice(0, key.length - 1);
      for (let i = 0; i < mods.length; i++) mods[i] = modifier[mods[i].toLowerCase()];
      return mods;
    }
    function getKeys(key) {
      if (typeof key !== 'string') key = '';
      key = key.replace(/\s/g, '');
      const keys = key.split(',');
      let index = keys.lastIndexOf('');
      for (; index >= 0;) {
        keys[index - 1] += ',';
        keys.splice(index, 1);
        index = keys.lastIndexOf('');
      }
      return keys;
    }
    function compareArray(a1, a2) {
      const arr1 = a1.length >= a2.length ? a1 : a2;
      const arr2 = a1.length >= a2.length ? a2 : a1;
      let isIndex = true;
      for (let i = 0; i < arr1.length; i++) {
        if (arr2.indexOf(arr1[i]) === -1) isIndex = false;
      }
      return isIndex;
    }
    const _keyMap = {
      backspace: 8,
      '⌫': 8,
      tab: 9,
      clear: 12,
      enter: 13,
      '↩': 13,
      return: 13,
      esc: 27,
      escape: 27,
      space: 32,
      left: 37,
      up: 38,
      right: 39,
      down: 40,
      arrowup: 38,
      arrowdown: 40,
      arrowleft: 37,
      arrowright: 39,
      del: 46,
      delete: 46,
      ins: 45,
      insert: 45,
      home: 36,
      end: 35,
      pageup: 33,
      pagedown: 34,
      capslock: 20,
      num_0: 96,
      num_1: 97,
      num_2: 98,
      num_3: 99,
      num_4: 100,
      num_5: 101,
      num_6: 102,
      num_7: 103,
      num_8: 104,
      num_9: 105,
      num_multiply: 106,
      num_add: 107,
      num_enter: 108,
      num_subtract: 109,
      num_decimal: 110,
      num_divide: 111,
      '⇪': 20,
      ',': 188,
      '.': 190,
      '/': 191,
      '`': 192,
      '-': isff ? 173 : 189,
      '=': isff ? 61 : 187,
      ';': isff ? 59 : 186,
      '\'': 222,
      '{': 219,
      '}': 221,
      '[': 219,
      ']': 221,
      '\\': 220
    };
    const _modifier = {
      '⇧': 16,
      shift: 16,
      '⌥': 18,
      alt: 18,
      option: 18,
      '⌃': 17,
      ctrl: 17,
      control: 17,
      '⌘': 91,
      cmd: 91,
      meta: 91,
      command: 91
    };
    const modifierMap = {
      16: 'shiftKey',
      18: 'altKey',
      17: 'ctrlKey',
      91: 'metaKey',
      shiftKey: 16,
      ctrlKey: 17,
      altKey: 18,
      metaKey: 91
    };
    const _mods = {
      16: false,
      18: false,
      17: false,
      91: false
    };
    const _handlers = {};
    for (let k = 1; k < 20; k++) {
      _keyMap["f".concat(k)] = 111 + k;
    }
    let _downKeys = [];
    let winListendFocus = null;
    let _scope = 'all';
    const elementEventMap = new Map();
    const code = x => _keyMap[x.toLowerCase()] || _modifier[x.toLowerCase()] || x.toUpperCase().charCodeAt(0);
    const getKey = x => Object.keys(_keyMap).find(k => _keyMap[k] === x);
    const getModifier = x => Object.keys(_modifier).find(k => _modifier[k] === x);
    function setScope(scope) {
      _scope = scope || 'all';
    }
    function getScope() {
      return _scope || 'all';
    }
    function getPressedKeyCodes() {
      return _downKeys.slice(0);
    }
    function getPressedKeyString() {
      return _downKeys.map(c => getKey(c) || getModifier(c) || String.fromCharCode(c));
    }
    function getAllKeyCodes() {
      const result = [];
      Object.keys(_handlers).forEach(k => {
        _handlers[k].forEach(_ref => {
          let {
            key,
            scope,
            mods,
            shortcut
          } = _ref;
          result.push({
            scope,
            shortcut,
            mods,
            keys: key.split('+').map(v => code(v))
          });
        });
      });
      return result;
    }
    function filter(event) {
      const target = event.target || event.srcElement;
      const {
        tagName
      } = target;
      let flag = true;
      const isInput = tagName === 'INPUT' && !['checkbox', 'radio', 'range', 'button', 'file', 'reset', 'submit', 'color'].includes(target.type);
      if (target.isContentEditable || (isInput || tagName === 'TEXTAREA' || tagName === 'SELECT') && !target.readOnly) {
        flag = false;
      }
      return flag;
    }
    function isPressed(keyCode) {
      if (typeof keyCode === 'string') {
        keyCode = code(keyCode);
      }
      return _downKeys.indexOf(keyCode) !== -1;
    }
    function deleteScope(scope, newScope) {
      let handlers;
      let i;
      if (!scope) scope = getScope();
      for (const key in _handlers) {
        if (Object.prototype.hasOwnProperty.call(_handlers, key)) {
          handlers = _handlers[key];
          for (i = 0; i < handlers.length;) {
            if (handlers[i].scope === scope) {
              const deleteItems = handlers.splice(i, 1);
              deleteItems.forEach(_ref2 => {
                let {
                  element
                } = _ref2;
                return removeKeyEvent(element);
              });
            } else {
              i++;
            }
          }
        }
      }
      if (getScope() === scope) setScope(newScope || 'all');
    }
    function clearModifier(event) {
      let key = event.keyCode || event.which || event.charCode;
      if (event.key && event.key.toLowerCase() === 'capslock') {
        key = code(event.key);
      }
      const i = _downKeys.indexOf(key);
      if (i >= 0) {
        _downKeys.splice(i, 1);
      }
      if (event.key && event.key.toLowerCase() === 'meta') {
        _downKeys.splice(0, _downKeys.length);
      }
      if (key === 93 || key === 224) key = 91;
      if (key in _mods) {
        _mods[key] = false;
        for (const k in _modifier) if (_modifier[k] === key) hotkeys[k] = false;
      }
    }
    function unbind(keysInfo) {
      if (typeof keysInfo === 'undefined') {
        Object.keys(_handlers).forEach(key => {
          Array.isArray(_handlers[key]) && _handlers[key].forEach(info => eachUnbind(info));
          delete _handlers[key];
        });
        removeKeyEvent(null);
      } else if (Array.isArray(keysInfo)) {
        keysInfo.forEach(info => {
          if (info.key) eachUnbind(info);
        });
      } else if (typeof keysInfo === 'object') {
        if (keysInfo.key) eachUnbind(keysInfo);
      } else if (typeof keysInfo === 'string') {
        for (var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
          args[_key - 1] = arguments[_key];
        }
        let [scope, method] = args;
        if (typeof scope === 'function') {
          method = scope;
          scope = '';
        }
        eachUnbind({
          key: keysInfo,
          scope,
          method,
          splitKey: '+'
        });
      }
    }
    const eachUnbind = _ref3 => {
      let {
        key,
        scope,
        method,
        splitKey = '+'
      } = _ref3;
      const multipleKeys = getKeys(key);
      multipleKeys.forEach(originKey => {
        const unbindKeys = originKey.split(splitKey);
        const len = unbindKeys.length;
        const lastKey = unbindKeys[len - 1];
        const keyCode = lastKey === '*' ? '*' : code(lastKey);
        if (!_handlers[keyCode]) return;
        if (!scope) scope = getScope();
        const mods = len > 1 ? getMods(_modifier, unbindKeys) : [];
        const unbindElements = [];
        _handlers[keyCode] = _handlers[keyCode].filter(record => {
          const isMatchingMethod = method ? record.method === method : true;
          const isUnbind = isMatchingMethod && record.scope === scope && compareArray(record.mods, mods);
          if (isUnbind) unbindElements.push(record.element);
          return !isUnbind;
        });
        unbindElements.forEach(element => removeKeyEvent(element));
      });
    };
    function eventHandler(event, handler, scope, element) {
      if (handler.element !== element) {
        return;
      }
      let modifiersMatch;
      if (handler.scope === scope || handler.scope === 'all') {
        modifiersMatch = handler.mods.length > 0;
        for (const y in _mods) {
          if (Object.prototype.hasOwnProperty.call(_mods, y)) {
            if (!_mods[y] && handler.mods.indexOf(+y) > -1 || _mods[y] && handler.mods.indexOf(+y) === -1) {
              modifiersMatch = false;
            }
          }
        }
        if (handler.mods.length === 0 && !_mods[16] && !_mods[18] && !_mods[17] && !_mods[91] || modifiersMatch || handler.shortcut === '*') {
          handler.keys = [];
          handler.keys = handler.keys.concat(_downKeys);
          if (handler.method(event, handler) === false) {
            if (event.preventDefault) event.preventDefault();else event.returnValue = false;
            if (event.stopPropagation) event.stopPropagation();
            if (event.cancelBubble) event.cancelBubble = true;
          }
        }
      }
    }
    function dispatch(event, element) {
      const asterisk = _handlers['*'];
      let key = event.keyCode || event.which || event.charCode;
      if (event.key && event.key.toLowerCase() === 'capslock') {
        return;
      }
      if (!hotkeys.filter.call(this, event)) return;
      if (key === 93 || key === 224) key = 91;
      if (_downKeys.indexOf(key) === -1 && key !== 229) _downKeys.push(key);
      ['metaKey', 'ctrlKey', 'altKey', 'shiftKey'].forEach(keyName => {
        const keyNum = modifierMap[keyName];
        if (event[keyName] && _downKeys.indexOf(keyNum) === -1) {
          _downKeys.push(keyNum);
        } else if (!event[keyName] && _downKeys.indexOf(keyNum) > -1) {
          _downKeys.splice(_downKeys.indexOf(keyNum), 1);
        } else if (keyName === 'metaKey' && event[keyName]) {
          _downKeys = _downKeys.filter(k => k in modifierMap || k === key);
        }
      });
      if (key in _mods) {
        _mods[key] = true;
        for (const k in _modifier) {
          if (Object.prototype.hasOwnProperty.call(_modifier, k)) {
            const eventKey = modifierMap[_modifier[k]];
            hotkeys[k] = event[eventKey];
          }
        }
        if (!asterisk) return;
      }
      for (const e in _mods) {
        if (Object.prototype.hasOwnProperty.call(_mods, e)) {
          _mods[e] = event[modifierMap[e]];
        }
      }
      if (event.getModifierState && !(event.altKey && !event.ctrlKey) && event.getModifierState('AltGraph')) {
        if (_downKeys.indexOf(17) === -1) {
          _downKeys.push(17);
        }
        if (_downKeys.indexOf(18) === -1) {
          _downKeys.push(18);
        }
        _mods[17] = true;
        _mods[18] = true;
      }
      const scope = getScope();
      if (asterisk) {
        for (let i = 0; i < asterisk.length; i++) {
          if (asterisk[i].scope === scope && (event.type === 'keydown' && asterisk[i].keydown || event.type === 'keyup' && asterisk[i].keyup)) {
            eventHandler(event, asterisk[i], scope, element);
          }
        }
      }
      if (!(key in _handlers)) return;
      const handlerKey = _handlers[key];
      const keyLen = handlerKey.length;
      for (let i = 0; i < keyLen; i++) {
        if (event.type === 'keydown' && handlerKey[i].keydown || event.type === 'keyup' && handlerKey[i].keyup) {
          if (handlerKey[i].key) {
            const record = handlerKey[i];
            const {
              splitKey
            } = record;
            const keyShortcut = record.key.split(splitKey);
            const _downKeysCurrent = [];
            for (let a = 0; a < keyShortcut.length; a++) {
              _downKeysCurrent.push(code(keyShortcut[a]));
            }
            if (_downKeysCurrent.sort().join('') === _downKeys.sort().join('')) {
              eventHandler(event, record, scope, element);
            }
          }
        }
      }
    }
    function hotkeys(key, option, method) {
      _downKeys = [];
      const keys = getKeys(key);
      let mods = [];
      let scope = 'all';
      let element = document;
      let i = 0;
      let keyup = false;
      let keydown = true;
      let splitKey = '+';
      let capture = false;
      let single = false;
      if (method === undefined && typeof option === 'function') {
        method = option;
      }
      if (Object.prototype.toString.call(option) === '[object Object]') {
        if (option.scope) scope = option.scope;
        if (option.element) element = option.element;
        if (option.keyup) keyup = option.keyup;
        if (option.keydown !== undefined) keydown = option.keydown;
        if (option.capture !== undefined) capture = option.capture;
        if (typeof option.splitKey === 'string') splitKey = option.splitKey;
        if (option.single === true) single = true;
      }
      if (typeof option === 'string') scope = option;
      if (single) unbind(key, scope);
      for (; i < keys.length; i++) {
        key = keys[i].split(splitKey);
        mods = [];
        if (key.length > 1) mods = getMods(_modifier, key);
        key = key[key.length - 1];
        key = key === '*' ? '*' : code(key);
        if (!(key in _handlers)) _handlers[key] = [];
        _handlers[key].push({
          keyup,
          keydown,
          scope,
          mods,
          shortcut: keys[i],
          method,
          key: keys[i],
          splitKey,
          element
        });
      }
      if (typeof element !== 'undefined' && window) {
        if (!elementEventMap.has(element)) {
          const keydownListener = function () {
            let event = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : window.event;
            return dispatch(event, element);
          };
          const keyupListenr = function () {
            let event = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : window.event;
            dispatch(event, element);
            clearModifier(event);
          };
          elementEventMap.set(element, {
            keydownListener,
            keyupListenr,
            capture
          });
          addEvent(element, 'keydown', keydownListener, capture);
          addEvent(element, 'keyup', keyupListenr, capture);
        }
        if (!winListendFocus) {
          const listener = () => {
            _downKeys = [];
          };
          winListendFocus = {
            listener,
            capture
          };
          addEvent(window, 'focus', listener, capture);
        }
      }
    }
    function trigger(shortcut) {
      let scope = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 'all';
      Object.keys(_handlers).forEach(key => {
        const dataList = _handlers[key].filter(item => item.scope === scope && item.shortcut === shortcut);
        dataList.forEach(data => {
          if (data && data.method) {
            data.method();
          }
        });
      });
    }
    function removeKeyEvent(element) {
      const values = Object.values(_handlers).flat();
      const findindex = values.findIndex(_ref4 => {
        let {
          element: el
        } = _ref4;
        return el === element;
      });
      if (findindex < 0) {
        const {
          keydownListener,
          keyupListenr,
          capture
        } = elementEventMap.get(element) || {};
        if (keydownListener && keyupListenr) {
          removeEvent(element, 'keyup', keyupListenr, capture);
          removeEvent(element, 'keydown', keydownListener, capture);
          elementEventMap.delete(element);
        }
      }
      if (values.length <= 0 || elementEventMap.size <= 0) {
        const eventKeys = Object.keys(elementEventMap);
        eventKeys.forEach(el => {
          const {
            keydownListener,
            keyupListenr,
            capture
          } = elementEventMap.get(el) || {};
          if (keydownListener && keyupListenr) {
            removeEvent(el, 'keyup', keyupListenr, capture);
            removeEvent(el, 'keydown', keydownListener, capture);
            elementEventMap.delete(el);
          }
        });
        elementEventMap.clear();
        Object.keys(_handlers).forEach(key => delete _handlers[key]);
        if (winListendFocus) {
          const {
            listener,
            capture
          } = winListendFocus;
          removeEvent(window, 'focus', listener, capture);
          winListendFocus = null;
        }
      }
    }
    const _api = {
      getPressedKeyString,
      setScope,
      getScope,
      deleteScope,
      getPressedKeyCodes,
      getAllKeyCodes,
      isPressed,
      filter,
      trigger,
      unbind,
      keyMap: _keyMap,
      modifier: _modifier,
      modifierMap
    };
    for (const a in _api) {
      if (Object.prototype.hasOwnProperty.call(_api, a)) {
        hotkeys[a] = _api[a];
      }
    }
    if (typeof window !== 'undefined') {
      const _hotkeys = window.hotkeys;
      hotkeys.noConflict = deep => {
        if (deep && window.hotkeys === hotkeys) {
          window.hotkeys = _hotkeys;
        }
        return hotkeys;
      };
      window.hotkeys = hotkeys;
    }

    const initHotkeys = () => {
        hotkeys.deleteScope('paginator');
        hotkeys.deleteScope('metseditor');
        hotkeys.deleteScope('global');
        hotkeys.unbind();
        const isMetseditor = document.querySelector('#metseditorMenuForm') !== null;
        const isPaginatorOnPage = document.querySelector('.dataTables__paginator') !== null || document.querySelector('[id$="navnext"]') !== null;
        if (isMetseditor) {
            hotkeys.setScope('metseditor');
        } else if (isPaginatorOnPage) {
            hotkeys.setScope('paginator');
        } else {
            hotkeys.setScope('global');
        }
        initHotkeysHelper();
        const configElement = document.getElementById('gwConfig');
        const goobiWorkflowConfig = configElement ? JSON.parse(configElement.textContent) : {};
        const hotkeysPrefix = goobiWorkflowConfig.navigationShortcut || '';
        const developerTools = document.querySelectorAll('[data-developer-tool]');
        if (developerTools.length > 0) {
            hotkeys(`${hotkeysPrefix}+d`, (event, handler) => {
                event.preventDefault();
                event.stopPropagation();
                developerTools.forEach((element) => {
                    element.classList.toggle('d-none');
                });
            });
        }
        if (hotkeys.getScope() !== 'paginator') return;
        hotkeys(`${hotkeysPrefix}+right`, 'paginator', () => {
            document.querySelector('[id$="navnext"]')?.click();
        });
        hotkeys(`${hotkeysPrefix}+left`, 'paginator', () => {
            document.querySelector('[id$="navprev"]')?.click();
        });
        hotkeys(`${hotkeysPrefix}+home`, 'paginator', () => {
            document.querySelector('[id$="navfirst"]')?.click();
        });
        hotkeys(`${hotkeysPrefix}+end`, 'paginator', () => {
            document.querySelector('[id$="navlast"]')?.click();
        });
    };
    const initHotkeysHelper = () => {
        const hotkeyTermsMetseditor = [
            'shortcut_imagePrev20',
            'shortcut_imageNext20',
            'shortcut_imagePrev1',
            'shortcut_imageNext1',
            'shortcut_imageFirst',
            'shortcut_imageLast',
            'shortcut_toggleCheckboxPagination',
            'shortcut_saveMets',
            'shortcut_validate'
        ];
        const hotkeyTermsPaginator = [
            'shortcut_imagePrev1',
            'shortcut_imageNext1',
            'shortcut_imageFirst',
            'shortcut_imageLast',
        ];
        const scope = hotkeys.getScope();
        const hotkeyHelpButton = document.querySelector('#shortcutsHelpButton');
        if (scope === 'global' && hotkeyHelpButton) {
            hotkeyHelpButton.classList.add('d-none');
            return;
        }
        if (hotkeyHelpButton) {
            hotkeyHelpButton.classList.remove('d-none');
        }
        if (scope === 'metseditor') {
            hotkeyTermsMetseditor.forEach(term => showHotkeyHelpers(term));
            return;
        }
        if (scope === 'paginator') {
            hotkeyTermsPaginator.forEach(term => showHotkeyHelpers(term));
            return;
        }
    };
    const showHotkeyHelpers = (hotkey) => {
        const hotkeyElement = document.querySelector(`[data-shortcut-helper="${hotkey}"]`);
        if (hotkeyElement) {
            const descriptionElement = hotkeyElement.parentElement;
            const termElement = descriptionElement.previousElementSibling;
            termElement.classList.remove('d-none');
            descriptionElement.classList.remove('d-none');
        }
    };

    const settings = {
        brightness: "#imageFiltersBrightness",
        contrast: "#imageFiltersContrast",
        saturation: "#imageFiltersSaturation",
        hue: "#imageFiltersHue",
        bitonal: "#imageFiltersBitonalSwitch",
        bitonalThreshold: "#imageFiltersBitonalThreshold",
        grayscale: "#imageFiltersGrayscale",
        invert: "#imageFiltersColorInversion",
        sharpening: "#imageFiltersSharpening",
        reset: "#imageFiltersReset"
    };
    let subscriptions = [];
    let filters = {};
    const initImageFilters = function(image) {
        if(!image || !image.element) {
            return;
        }
        subscriptions.forEach(s => s.unsubscribe());
        subscriptions = [];
        Object.entries(filters).forEach(([key, value]) => {
            value.close();
        });
        if(document.querySelector(settings.brightness)) {
            filters.brightness = new ImageView.ImageFilters.Brightness(image, 0);
            filters.brightness.elements = document.querySelectorAll(settings.brightness);
            filters.brightness.baseValue = 0;
            subscriptions.push( rxjs.fromEvent( filters.brightness.elements, "input").subscribe(e => _handleInput(e, filters.brightness)));
        }
        if(document.querySelector(settings.contrast)) {
            filters.contrast = new ImageView.ImageFilters.Contrast(image, 1);
            filters.contrast.elements = document.querySelectorAll(settings.contrast);
            filters.contrast.baseValue = 1;
            subscriptions.push( rxjs.fromEvent( filters.contrast.elements, "input").subscribe(e => _handleInput(e, filters.contrast)));
        }
        if(document.querySelector(settings.saturation)) {
            filters.saturation = new ImageView.ImageFilters.ColorSaturation(image, 1);
            filters.saturation.elements = document.querySelectorAll(settings.saturation);
            filters.saturation.baseValue = 1;
            subscriptions.push( rxjs.fromEvent( filters.saturation.elements, "input").subscribe(e => _handleInput(e, filters.saturation)));
        }
        if(document.querySelector(settings.hue)) {
            filters.hue = new ImageView.ImageFilters.ColorRotate(image, 0);
            filters.hue.elements = document.querySelectorAll(settings.hue);
            filters.hue.baseValue = 0;
            subscriptions.push( rxjs.fromEvent( filters.hue.elements, "input").subscribe(e => _handleInput(e, filters.hue)));
        }
        if(document.querySelector(settings.bitonal)) {
            filters.bitonal = new ImageView.ImageFilters.Threshold(image, 128);
            filters.bitonal.elements = Array.from(document.querySelectorAll(settings.bitonal)).concat(Array.from(document.querySelectorAll(settings.bitonalThreshold)));
            filters.bitonal.baseValue = 128;
            subscriptions.push( rxjs.fromEvent( document.querySelectorAll(settings.bitonal), "change").subscribe(e => _handleInput(e, filters.bitonal, [filters.grayscale, filters.sharpening])));
            subscriptions.push( rxjs.fromEvent( document.querySelectorAll(settings.bitonalThreshold), "input").subscribe(e => _handleInput(e, filters.bitonal, [filters.grayscale, filters.sharpening])));
        }
        if(document.querySelector(settings.grayscale)) {
            filters.grayscale = new ImageView.ImageFilters.Grayscale(image);
            filters.grayscale.elements = document.querySelectorAll(settings.grayscale);
            subscriptions.push( rxjs.fromEvent(filters.grayscale.elements, "change").subscribe(e => _handleInput(e, filters.grayscale, [filters.bitonal])));
        }
        if(document.querySelector(settings.invert)) {
            filters.invert = new ImageView.ImageFilters.Invert(image);
            filters.invert.elements = document.querySelectorAll(settings.invert);
            subscriptions.push( rxjs.fromEvent( filters.invert.elements, "change").subscribe(e => _handleInput(e, filters.invert)));
        }
        if(document.querySelector(settings.sharpening)) {
            filters.sharpening = new ImageView.ImageFilters.Sharpen(image, 1);
            filters.sharpening.elements = document.querySelectorAll(settings.sharpening);
            subscriptions.push( rxjs.fromEvent( document.querySelectorAll(settings.sharpening), "change").subscribe(e => _handleInput(e, filters.sharpening, [filters.bitonal])));
        }
        if(document.querySelector(settings.reset)) {
            subscriptions.push( rxjs.fromEvent( document.querySelectorAll(settings.reset), "click").subscribe(e => _resetFilters(filters)));
        }
        return filters;
    };
    function _closeFilter(filter) {
        filter.close();
        filter.elements.forEach(element => {
            if(element.type === "checkbox") {
                element.checked = false;
            } else {
                element.value = filter.baseValue;
            }
        });
    }
    function _startFilter(filter) {
        filter.start();
        filter.elements.forEach(element => {
            if(element.type === "checkbox") {
                element.checked = true;
            }
        });
    }
    function _resetFilters(imageFilters) {
        _closeFilter(imageFilters.brightness);
        _closeFilter(imageFilters.contrast);
        _closeFilter(imageFilters.saturation);
        _closeFilter(imageFilters.hue);
        _closeFilter(imageFilters.bitonal);
        _closeFilter(imageFilters.grayscale);
        _closeFilter(imageFilters.invert);
        _closeFilter(imageFilters.sharpening);
    }
    function _handleInput(event, filter, precludes) {
        const value = event.target.value;
        if(!filter.isActive()) {
            _startFilter(filter);
            precludes?.forEach(preclude => _closeFilter(preclude));
        } else if(isNaN(value) ) {
            _closeFilter(filter);
        }
        if(!isNaN(value) ) {
            filter.setValue(parseFloat(value));
        }
    }

    if (!window.__initFunctionsCallCount) {
        window.__initFunctionsCallCount = 0;
        window.__totalListenersAdded = 0;
        const origAdd = EventTarget.prototype.addEventListener;
        EventTarget.prototype.addEventListener = function(type, fn, opts) {
            window.__totalListenersAdded++;
            return origAdd.call(this, type, fn, opts);
        };
    }
    const initFunctions = function initFunctions() {
        const before = window.__totalListenersAdded;
        window.__initFunctionsCallCount++;
        init$1();
        init();
        dropdownTooltips();
        initScrollTop();
        initFocusOnClick();
        initSubmitOnEnter();
        initMirrorValueTo();
        initAccessibilityModeButton();
        initHotkeys();
        focusOnLoad();
        toggleHelp();
        const zoomableImage = loadImage();
        initImageAreaCreation(zoomableImage);
        initEditImageAreas(zoomableImage);
        initImageFilters(zoomableImage);
        const added = window.__totalListenersAdded - before;
        console.warn(
            `[LEAK-DEBUG] initFunctions() call #${window.__initFunctionsCallCount}: ` +
            `+${added} listeners this call, ${window.__totalListenersAdded} total`
        );
    };

    const toggleLoaders = function toggleLoaders(show) {
        const loaders = document.querySelectorAll('.ajax-loader');
        if (loaders.length === 0) return;
        const displayValue = show ? 'block' : 'none';
        loaders.forEach(loader => {
            loader.style.display = displayValue;
        });
    };

    let cachedScrollPositions = null;
    const getScrollPositions = () => {
        if (cachedScrollPositions === null) {
            const stored = sessionStorage.getItem('gwScrollPositions');
            cachedScrollPositions = stored ? JSON.parse(stored) : {};
        }
        return cachedScrollPositions;
    };
    const persistScrollPositions = () => {
        if (cachedScrollPositions !== null) {
            sessionStorage.setItem('gwScrollPositions', JSON.stringify(cachedScrollPositions));
        }
    };
    const saveScrollPosition = (element) => {
        const id = element.id;
        if (!id) {
            return;
        }
        const scrollPositions = getScrollPositions();
        scrollPositions[id] = element.scrollTop;
    };
    const restoreScrollPosition = (element) => {
        const id = element.id;
        if (!id) {
            return;
        }
        const scrollPositions = getScrollPositions();
        if (scrollPositions[id] !== undefined) {
            const scrollTop = scrollPositions[id];
            if (element.querySelector('canvas')) {
                document.addEventListener('thumbnailsLoaded', () => {
                    element.scrollTo({ top: scrollTop, behavior: 'smooth' });
                }, { once: true });
            } else {
                element.scrollTo({ top: scrollTop, behavior: 'smooth' });
            }
        }
    };
    const restoreAllScrollPositions = () => {
        requestAnimationFrame(() => {
            document.querySelectorAll('[data-save-scroll-position]').forEach(element => {
                restoreScrollPosition(element);
            });
        });
    };
    const saveAllScrollPositions = () => {
        document.querySelectorAll('[data-save-scroll-position]').forEach(element => {
            saveScrollPosition(element);
        });
        persistScrollPositions();
    };
    const initSaveScrollPosition = () => {
        document.querySelectorAll('[data-save-scroll-positions]').forEach(button => {
            button.addEventListener('click', () => {
                saveAllScrollPositions();
            });
        });
    };
    const resetScrollPosition = (elementId) => {
        const scrollPositions = getScrollPositions();
        if (scrollPositions[elementId] !== undefined) {
            delete scrollPositions[elementId];
            persistScrollPositions();
        }
    };
    const resetAllScrollPositions = () => {
        cachedScrollPositions = {};
        sessionStorage.removeItem('gwScrollPositions');
    };
    const handleScrollPositionReset = (resetAttr) => {
        if (!resetAttr) return;
        ['all', 'true'].includes(resetAttr)
            ? resetAllScrollPositions()
            : resetAttr.split(',').forEach(id => resetScrollPosition(id.trim()));
    };

    let config = null;
    let autosaveInterval = null;
    let readOnlyMode = null;
    let autosaveTimer = null;
    const setupAutosave = () => {
        config = JSON.parse(document.getElementById('gwConfig').textContent);
        autosaveInterval = config.autoSaveInterval * 1000 * 60;
        readOnlyMode = config.readOnlyMode === 'true';
    };
    const initAutosave = () => {
        if (config === null || autosaveInterval === null || readOnlyMode === null) {
            setupAutosave();
        }
        if (!readOnlyMode && autosaveInterval > 0) {
            clearInterval(autosaveTimer);
            autosaveTimer = setInterval(() => {
                const saveButton = document.querySelector('[id$="meMenuActionsForm:automaticSave"]');
                if (saveButton) {
                    saveButton.click();
                }
            }, autosaveInterval);
        }
    };

    class AjaxButtonManager {
        static ATTR_DISABLE_ON_AJAX = 'data-disable-on-ajax';
        static ATTR_AJAX_GROUP = 'data-ajax-group';
        static ATTR_PROCESSING = 'data-ajax-processing';
        constructor() {
            this.initialized = false;
            this.init();
        }
        init() {
            if (this.initialized) return;
            if (typeof faces !== 'undefined' && faces.ajax) {
                faces.ajax.addOnEvent(this.handleAjaxEvent.bind(this));
                this.initialized = true;
            }
        }
        handleAjaxEvent(data) {
            const status = data.status;
            const source = data.source;
            if (!source) return;
            if (status === 'begin') {
                this.handleAjaxBegin(source);
            } else if (status === 'complete') {
                this.handleAjaxComplete(source);
            }
        }
        handleAjaxBegin(sourceElement) {
            const disableStrategy = sourceElement.getAttribute(AjaxButtonManager.ATTR_DISABLE_ON_AJAX);
            if (!disableStrategy) return;
            const buttonsToDisable = this.getRelevantButtons(sourceElement, disableStrategy);
            this.disableButtons(buttonsToDisable);
        }
        handleAjaxComplete(sourceElement) {
            const disableStrategy = sourceElement.getAttribute(AjaxButtonManager.ATTR_DISABLE_ON_AJAX);
            if (!disableStrategy) return;
            const buttonsToEnable = this.getRelevantButtons(sourceElement, disableStrategy);
            this.enableButtons(buttonsToEnable);
        }
        getRelevantButtons(sourceElement, strategy) {
            switch (strategy) {
                case 'self':
                    return [sourceElement];
                case 'form':
                    return this.getFormButtons(sourceElement);
                case 'group':
                    return this.getGroupButtons(sourceElement);
                default:
                    return [];
            }
        }
        getFormButtons(sourceElement) {
            const form = sourceElement.closest('form');
            if (!form) return [];
            const buttons = form.querySelectorAll('button, input[type="button"], input[type="submit"]');
            return Array.from(buttons);
        }
        getGroupButtons(sourceElement) {
            const groupName = sourceElement.getAttribute(AjaxButtonManager.ATTR_AJAX_GROUP);
            if (!groupName) return [];
            const buttons = document.querySelectorAll(`[${AjaxButtonManager.ATTR_AJAX_GROUP}="${groupName}"]`);
            return Array.from(buttons);
        }
        disableButtons(buttons) {
            buttons.forEach(btn => {
                if (!btn.disabled) {
                    btn.disabled = true;
                    btn.setAttribute(AjaxButtonManager.ATTR_PROCESSING, 'true');
                }
            });
        }
        enableButtons(buttons) {
            console.log('Enabling buttons:', buttons);
            buttons.forEach(btn => {
                if (btn.getAttribute(AjaxButtonManager.ATTR_PROCESSING) === 'true') {
                    btn.disabled = false;
                    btn.removeAttribute(AjaxButtonManager.ATTR_PROCESSING);
                }
            });
        }
    }

    new AjaxButtonManager();
    document.addEventListener('DOMContentLoaded', () => {
        initFunctions();
        initSaveScrollPosition();
        restoreAllScrollPositions();
        initAutosave();
    });
    if(typeof faces !== "undefined") {
        faces.ajax.addOnEvent((data) => {
            if(data.source?.dataset?.ajaxBehaviour === "ignore") {
                    return;
                }
            switch (data.status) {
                case 'begin':
                    toggleLoaders(true);
                    saveAllScrollPositions();
                    break;
                case 'complete':
                    break;
                case 'success':
                    toggleLoaders(false);
                    initFunctions();
                    initSaveScrollPosition();
                    handleScrollPositionReset(data.source?.getAttribute('data-reset-scroll-positions'));
                    restoreAllScrollPositions();
                    initAutosave();
                    break;
            }
        });
    } else {
        console.warn("No Jakarta faces initialized");
    }

})();
